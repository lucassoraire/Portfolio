---
const { title, description, link, image } = Astro.props;
---

<div class="bg-white rounded-2xl shadow-lg hover:shadow-2xl transition-shadow duration-300 p-6 group hover:scale-[1.02] transform cursor-pointer max-w-sm mx-auto">
  {image && (
    <img src={image} alt={`Imagen del proyecto ${title}`} class="rounded-xl mb-4 object-cover w-full h-48" />
  )}
  <h2 class="text-xl font-bold text-gray-800 group-hover:text-sky-900 transition-colors duration-300">{title}</h2>
  <p class="mt-2 text-gray-600 whitespace-pre-line">{description}</p>
  <a href={link} class="inline-block mt-4 text-sky-900 font-medium hover:underline">Ver proyecto →</a>
</div>

